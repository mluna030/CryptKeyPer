pub mod hash_function;
pub mod drbg;
pub mod random_key_generator;
pub mod mnemonic;
pub mod xmss;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
